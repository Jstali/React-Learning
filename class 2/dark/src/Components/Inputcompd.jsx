import React from "react";

const Inputcompd = ({ label, type }) => {
  return (
    <div>
      <label htmlFor="">{label}</label>
      <input type={type} name="" id="" />
    </div>
  );
};

export default Inputcompd;
